———设计模式病：如果不是为了让类在未来可以拓展 满足当下需求即可无需过度解耦合 太累
结构型 行为型 解决写类时的耦合问题——分析的时候要分为 对象与类//对象与对象
适配器模式：
    NormalOut是会被播放器识别的输出接口
    AdvanceOut则是别的输出接口，识别NormalOut的播放器无法识别
    SimpleAdapter 是具体实现

装饰器模式的运用：
    多次的音量调节(一层套一层)